# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  displayName: run
  inputs: 
    command: run
    arguments: '-t --network=host -e ACCEPT_EULA=true parasoft/soavirt ping 34.212.121.186'